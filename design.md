# Uplus Icon Web Design Language

本文档定义 Uplus Icon 网站的视觉语言和页面实现约束。基准来源为 Figma 文件 `HCcleuKKkwCak6qzLKvrQR` 的节点 `41:3532`。设计稿未直接覆盖的页面，应使用本文档延展，不应恢复旧版营销式视觉。

## 1. 设计原则

- 工具优先：界面服务于查找、理解和使用图标，不制造额外装饰层级。
- 内容优先：白色背景承载内容，浅灰只表达控件、选中、悬停和代码区域。
- 紧凑清晰：使用固定基元和稳定间距，不使用夸张标题、厚重阴影或大面积卡片。
- 单一真源：所有被展示的正式图标来自 `@uplus-icon/react`；导航与工具栏使用单独审核的 UI 图标组件，不进入正式图标资产库。
- 状态明确：选中项使用白色内层或 `#f0f0f0` 背景，未选中项使用 `#8e8e8e`。

## 2. 基础 Token

### 颜色

| Token | 亮色 | 用途 |
|---|---|---|
| `--ui-bg` | `#ffffff` | 页面背景 |
| `--ui-text` | `#000000` | 标题、正文、图标 |
| `--ui-muted` | `#8e8e8e` | 次要文字、计数、占位符 |
| `--ui-surface` | `#f1f1f1` | 控件外层、代码面板 |
| `--ui-hover` | `#f0f0f0` | 悬停、选中图标单元 |
| `--ui-subtle` | `#f7f7f7` | 预览区、次级内容面 |
| `--ui-line` | `#f1f1f1` | 边界和分隔线 |

暗色模式保持相同语义，不直接反转颜色：背景使用 `#11110f`，操作面使用 `#242421`，选中面使用 `#3a3a35`，正文使用 `#f3f3ef`。

### 字体

- 界面字体：拉丁字符使用 `Inter`，中文使用 `Noto Sans SC`，最后回退到系统无衬线字体。
- 代码字体：使用系统等宽字体栈，避免额外加载第三套网页字体。
- 常用字号：12、14、16、20、24、32px。
- 正文默认 16px / 1.7；辅助文字 14px；计数 16px。
- 内容页标题使用 32px；首页 Hero 可使用 64–104px 展示标题，但必须限制宽度并承担唯一视觉焦点。

### 尺寸

- 图标视觉尺寸：24×24px。
- 图标点击区域：48×48px。
- 标准控件高度：40px。
- 分段控件内层：32×32px。
- 桌面页面边距：48px。
- 移动页面边距：20px。

### 间距

- 基础序列：4、8、12、16、24、32、48、64、96px。
- 同组控件：4–12px。
- 图标网格：16px。
- 页面区块：32–48px。
- 大章节：64–96px。

### 圆角

- 8px：输入框、分段控件内层。
- 10px：图标点击区域、代码块。
- 12px：分段控件外层、主要内容面板。
- 不使用胶囊圆角作为主要页面语言；仅允许状态标签使用。

## 3. 组件规则

### 分段控件

- 外层高度 40px，背景 `--ui-surface`，圆角 12px，内边距 4px。
- 内层高度 32px，圆角 8px。
- 选中项为白色；未选中项使用次要色。
- 主导航、主题、语言和布局切换共用可测量宽度的滑块选中态，切换时平滑移动到当前项目。

### 图标单元

- 固定 48×48px，内部图标 24×24px。
- 默认透明，无描边。
- hover、focus 和选中使用 `--ui-hover`。

### 页面标题

- 上方使用 14px 次级标签表示路径或类型。
- 标题使用 32px / 1.2。
- 描述限制在 720px，使用次级色。
- 状态和动作位于同一水平层级，移动端允许换行。

### 内容面板

- 默认使用页面背景，不为了分组而制造卡片。
- 只有交互预览、代码、控制区域使用浅灰操作面。
- 图标网格单元固定为 48px，横向间距以 16px 为下限并弹性分配剩余空间；容器达到或低于下一列阈值时自动增减列数，保持两端对齐。
- 面板间使用 16px 间距或 `--ui-line` 分隔。
- 不使用阴影。

### 交互状态

- 所有 Button 与可展开 Summary 必须具备 Hover、Pressed、Focus 与 Disabled 状态，不允许只实现点击逻辑。
- Hover 使用组件对应的 `--ui-hover`、文字色或主按钮透明度表达；Pressed 由 GSAP 提供 `0.97` 缩放与回弹，Hover 动效使用 `-1px` 上移。
- 鼠标、触屏和键盘输入必须进入同一套状态逻辑；`prefers-reduced-motion` 下关闭位移与缩放，但保留颜色和背景反馈。
- Disabled 控件降低透明度、使用不可用光标，并退出 GSAP 交互目标。

## 4. 页面结构

### 首页

- Hero 桌面端使用“信息与操作 + 互动图标场”左右结构，移动端按文案、操作、互动区顺序单列展示。
- 安装命令与浏览入口位于标题之后，不悬浮覆盖互动内容。
- 互动图标场的外层使用 `--ui-subtle`，内部使用 `--ui-bg`，禁止点阵、网格纹理、渐变装饰和独立灰色体系。
- 物理图标效果只承担产品演示，不作为文案背景，不影响标题与操作的可读性。

### 图标浏览页

- 顶部工具栏包含搜索、排序和“完全平铺 / 集合形式”切换。
- 工具栏作为完整组件从初始位置开始 sticky，搜索、Select 和布局切换必须同步固定，不允许独立漂移。
- 排序 Select 使用独立下拉组件，统一触发器、菜单、上下 Chevron、选中态和键盘交互，不使用浏览器原生弹出菜单。
- 图标页不叠加 Header 与 Toolbar 两层渐变：Header 范围保持主题实色，Toolbar 本体保持不透明，只在 Toolbar 下边缘向内容渐隐。
- Header 主导航以视口中心为基准定位，不受品牌或右侧工具区域宽度影响。
- 完全平铺直接渲染全部图标网格。
- 集合形式按 `categories[0]` 分组，每组包含标题、数量和图标网格。

### 图标详情抽屉

- 点击图标后保留浏览页作为上下文，从底部打开详情抽屉，不切换到独立页面。
- `/icons/:name` 仍作为可分享地址，直接访问时呈现“浏览页 + 抽屉”。
- 抽屉是用于“确认、调整、复制”的轻量图标工作台；点击遮罩、关闭按钮或按 `Esc` 关闭。
- 底部固定区只保留代码格式 Segment 和复制按钮；完整代码容器默认藏在工具条后方，不长期挤占阅读空间。
- 底部环境渐变、代码容器与固定工具条必须建立独立层级：渐变在最底层，代码容器保持完整不透明，工具条位于最上层；渐变不得覆盖或染入代码内容。
- 内容滚动进入最后 160px 时，代码容器按滚动进度由下向上逐渐展露，到达底部时完整显示；向上滚动时按相反顺序收回。
- 内容区必须为代码容器预留安全空间，揭示过程不改变抽屉布局高度，也不遮挡最后一段正文。
- 顶栏与滚动内容、滚动内容与底部操作栏之间使用 24px 背景透明度渐变，避免固定区域形成硬切割；渐变层不得拦截交互。
- 顶栏只保留图标名称、辅助名称和关闭操作，不展示规格、组件名或状态标签。
- 预览调试区使用单一圆角矩形和统一背景：左侧直接承载图标，右侧集中参数；不得再为图标增加独立画板、边框、不同底色或嵌套卡片。
- 模式 Segment 将两种语义分开：“母版”固定放大 24×24 源文件；“实际尺寸”按 CSS 像素显示组件并提供 Size 和绝对线宽。网格与骨骼固定在模式 Segment 下方，对两种模式均可用且切换模式时保持状态。
- 布尔参数使用左侧文本、右侧胶囊 Switch，不使用带图标的 Toggle Button；滑块位移动效使用 GSAP。
- Size、描边和绝对线宽必须同步到预览与当前复制格式；切回母版模式时输出确定性恢复为 24px 和普通缩放描边，但保留实际尺寸模式的上次参数。
- 网格与骨骼是可叠加的只读检查层。骨骼降低成品图标透明度并显示轮廓中心线、路径锚点、贝塞尔控制点和控制柄；它只解析生成后的 `IconDefinition`，不允许改写或反向生成正式 SVG。
- 24×24 检查网格必须由内部单位格线与独立闭合外框组成；外框向画布内缩半个描边宽度，确保四边完整且不会被 SVG 视口裁切。
- 画布、渲染类型、主分类与版本作为常用规格直接展示；标签、别名和完整分类收纳在“更多信息”中。
- 底部在 React、SVG、Web Component 三种格式间切换，并始终只提供一个与当前格式一致的主复制操作；顶部名称可直接复制。

### 文档页

- 桌面使用文档索引、正文、本页目录三栏。
- 右侧本页目录不显示额外标题，仅将当前阅读位置显示为主文字色。
- 文档索引选中项使用浅灰 8px 圆角状态。
- 文档索引的组标题、编号与条目统一使用 12px 水平基线；任何选中、折叠或移动端状态不得改变这条起始线。
- 桌面左右导航的初始位置必须与 sticky 锁定位置一致，正文滚动时侧栏不得先产生垂直位移；sticky 侧栏本身不应用进入位移动画。
- 正文最大宽度 760px。
- H1 为 32px，H2 为 24px，H3 为 18px。
- 代码、表格和引用使用浅灰内容面，不使用阴影。

### 更新日志

- 仓库根目录的 `CHANGELOG.md` 与 `CHANGELOG.zh-CN.md` 是更新日志唯一内容源，网站不得维护副本。
- `/changelog` 使用文档页面骨架，但不是规则文档；它必须作为左侧导航最顶部的独立入口，位于所有规则分组之前。桌面右侧目录改为版本索引，平板与移动端使用专用 Select 定位版本。
- 更新日志入口使用独立的开发版本模块样式，只展示名称与 `DEV` 状态；规则条目仍保持编号列表，两者不得混用同一种信息层级。
- 桌面文档索引内部滚动时，更新日志入口作为独立 sticky 头部恒常停留在顶部；其下缘使用 24px 主题背景透明度渐变承接滚动内容，渐变必须收在导航宽度内部且不得拦截交互。导航只允许纵向滚动，所有层级必须设置明确的最小宽度约束，禁止横向拖动、回弹或状态切换造成的左右位移。移动端导航没有独立滚动区，不套用该 sticky 行为。
- 文档规则与更新日志共用持久的导航外壳，二者切换时只替换正文与右侧目录，不得重新挂载导航或重置其滚动、展开状态。
- 开发快照使用 `x.y.z-dev.n`，必须明确标记尚未发布到 npm；正式发包后再使用标准 SemVer。
- 日志只记录用户可感知的 Added、Changed、Fixed、Breaking 与 Known issues，不罗列原始提交记录。

## 5. 响应式

- Header 固定在视口顶部；桌面高度 64px，移动端两行高度 112px，页面内容预留对应空间。Header 背景从主题实色向下渐变至透明，避免与正文形成硬切割。
- `> 1200px`：完整三栏文档；详情使用居中的底部宽抽屉。
- `761–1200px`：文档隐藏右侧目录；详情抽屉保持单一预览工作区并压缩参数布局。
- `<= 760px`：Header 使用两行结构；页面边距 20px；详情抽屉从底部展开并接近全屏，参数改为单列，复制操作固定在底部，文档为单栏。
- 任何断点不得产生横向滚动。

## 6. 动效与无障碍

- 控件状态遵循“默认—Hover/Focus—Selected/Pressed/Expanded—Selected + Hover”四级颜色层级：Hover 是短暂的弱反馈，持久状态使用 `--ui-selected`，选中后再次 Hover 使用 `--ui-selected-hover`；禁止任意两个相邻状态使用相同背景和文字组合。
- Segment 通过滑块表达选中，Switch 通过轨道与滑块表达选中；已有明确结构状态的控件不得再叠加不必要的重背景。
- 全站动效统一使用 GSAP，CSS 不维护 `transition`、`animation` 或 `@keyframes`。
- 微交互控制在 160–220ms，抽屉等容器动效控制在 180–340ms，不使用弹性或大幅位移。
- 有进入动效的临时界面必须提供对应退出动效，并在退出时间线完成后再卸载或切换路由。
- 遵循 `prefers-reduced-motion`。
- 所有操作优先使用原生 button、input、a 等语义元素；自定义 Select 必须提供 listbox 语义和完整键盘行为。
- 键盘焦点必须清晰可见，点击区域不得小于 40px。
- 装饰图标隐藏于辅助技术；语义图标必须由按钮或链接提供可访问名称。

## 7. 资产边界

- 禁止修改 `packages/icons/raw/**/*.svg`。
- 禁止从截图重绘正式图标。
- Figma 临时资源不得成为长期生产依赖。
- 已审核的工具栏图标通过 `UiIconSlot` 与 `UiIcon` 接入，统一使用透明 24×24 画布和 `currentColor`。
