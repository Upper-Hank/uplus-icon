# Uplus Icon 项目协作规范

## 项目目标

Uplus Icon 是 Uplus 的官方图标系统，包含 React 图标组件库和配套文档网站。

项目目标：

- 建立统一、稳定、适合生产环境的图标资产库。
- 为业务提供类型安全、易用且可按需加载的 React API。
- 提供图标搜索、浏览、预览、复制和使用说明。
- 保证图标资产、组件代码与文档数据来自同一个事实来源。
- 在不破坏现有 API 的前提下持续扩充图标和工程能力。

## 最高优先级：原始 SVG 保护规则

`packages/icons/raw/**/*.svg` 是由项目负责人提供并确认的正式图标源文件。

任何 Agent 或自动化工具都必须遵守以下规则：

1. 禁止直接修改、重绘、优化、格式化或覆盖任何原始 SVG。
2. 禁止自行调整 SVG 的 path、坐标、尺寸、viewBox、描边、填充、圆角、颜色、节点顺序或视觉细节；唯一例外是下述已授权的发布适配，不得反向写入真源。
3. 禁止使用 SVGO、格式化器、批处理脚本或其他工具对原始 SVG 执行写入操作。
4. 禁止根据截图、其他图标库或主观判断自行生成正式图标并放入 `packages/icons/raw`。
5. 禁止为了通过构建或测试而自动修复原始 SVG。
6. 新增或替换图标时，只能使用项目负责人明确提供或明确批准的成熟 SVG 文件。
7. 如果原始 SVG 存在错误、不一致或无法构建，应停止对该文件的修改，准确报告问题并等待项目负责人提供新文件或授权处理。
8. 未获得针对具体文件的明确授权时，即使改动看似无损，也一律视为禁止。

生成脚本可以只读解析原始 SVG，并将结果写入生成目录或构建产物；不得反向写入 `packages/icons/raw`。设计真源与发布产物采用双层颜色模型：真源必须使用固定 `black` 与 `none`；私有适配阶段只允许把属性值 `black` 确定性替换为 `currentColor`，`none` 保持不变。适配不得改变 path、坐标、尺寸、viewBox、描边宽度、圆角、节点顺序或其他视觉和结构内容。

### SVG 准入与生成规则

- 带背景、网格、参考框、中心线或其他辅助线的设计稿不是正式真源，必须先在仓库外完成清理和审核。
- 正式设计真源使用透明背景，视觉颜色只使用固定 `black` 与 `none`；`currentColor` 只存在于生成后的运行时定义和发布产物中。
- Core、React、Web 和网站只能消费适配后的 `currentColor` 定义，不得直接发布或运行时消费固定黑色真源，也不得维护第二份人工 SVG。
- 天然线条优先保留 `stroke`，不得为了生成包体而统一转成填充轮廓。
- 允许基础 SVG 图形和已经烘焙进 path 的复合几何；正式真源不得依赖运行时 `mask`、`clipPath`、`filter`、外部资源或脚本。
- 不能安全消除的遮罩、裁剪或布尔结构必须停止导入，等待负责人提供成熟文件或明确授权处理。
- 完整规则正文见 `docs/rules`；`docs/icon-architecture.md` 是兼容目录入口。

## 目录职责

- `packages/icons/raw`：正式 SVG 源文件，只读资产。
- `packages/icons/metadata`：与 SVG 分离维护的分类、标签、别名等数据。
- `packages/icons/scripts`：只读处理 SVG 并生成代码的私有工具包。
- `packages/core`：框架无关的类型、元数据和 SVG definition 公共包。
- `packages/react`：React 组件和公共 API。
- `packages/web`：原生 DOM API 和 Web Component 公共包。
- `packages/*/src/generated`：由生成脚本产生的代码，不手工编辑。
- `packages/*/dist`：构建产物，不作为源代码直接维护。
- `apps/site`：图标库文档和展示网站。

## 产品需求

### 图标组件库

- 支持具名 React 组件，例如 `CheckIcon`。
- 支持通过名称渲染，例如 `<Icon name="check" />`。
- 提供完整的 TypeScript 类型。
- 支持标准 SVG 属性、`ref` 和尺寸，并忠实保留负责人提供的 SVG 渲染内容。
- 提供范围为 `0.5–2` 的 `weight` API；默认值为 `2`，越界输入向边界收敛，并保持每个图标内部的原始比例。`absoluteWeight` 默认关闭；开启后仅对可确定的数值尺寸抵消 SVG 尺寸缩放，字符串尺寸安全回退为相对重量。
- 保持 API 简洁、行为明确并兼容服务端渲染。
- 确保单个图标可以真正按需打包，避免导入一个图标时包含完整图标集。
- 生成代码必须稳定、可复现，不依赖手工修补。

### 文档网站

- 支持图标浏览、搜索、详情查看和使用示例。
- 支持中英文和明暗主题。
- 支持复制 React 用法、图标名称和 SVG 源码。
- 后续可以增加分类、标签、别名、筛选和不同尺寸预览。
- 网站展示必须使用正式图标资产，不得维护另一套手工复制的图标。

### 图标元数据

- 图标名称、分类、标签、别名、状态等元数据应与 SVG 文件分离维护。
- 修改元数据不得改写或格式化原始 SVG。
- 图标改名应考虑兼容性，必要时通过别名保留旧名称。
- 生成产物不是元数据的人工维护入口。
- `packages/icons/metadata/categories.json` 是分类 ID、排序和中英文名称的唯一注册表。
- 每个正式 SVG 必须在 `icons.json` 中拥有完整元数据，至少包含一个分类和一个标签。
- `categories[0]` 是展示主分组，其余分类只用于交叉筛选；网站不得维护第二份分组清单。
- 标签用于检索，别名用于同义词和历史叫法检索；别名不会自动产生公共 API 导出。
- 分类 ID 必须稳定，调整分类词表或主分组时应同步生成结果和展示网站。

## 工程要求

- 优先选择简单、低复杂度、易维护的生产级方案。
- 小功能不要引入大型依赖、复杂抽象或不必要的中间层。
- 修改前先了解现有 API、生成流程和目录职责。
- 不手工编辑生成文件；应修改生成器或源代码后重新生成。
- 新功能应包含与风险相称的类型检查、测试或构建验证。
- 涉及打包结构时，应验证 Tree Shaking 和单图标产物体积。
- 保持无障碍行为：装饰图标默认隐藏，有语义的图标应支持可访问名称。
- 不在未经确认的情况下执行破坏性迁移、批量改名或删除图标。

## Agent 工作边界

- 可以修改组件代码、类型、生成脚本、测试、构建配置和文档网站。
- 可以只读检查原始 SVG，并报告问题。
- 可以创建独立的元数据文件，但不得借此修改 SVG 内容。
- 涉及新增、替换、修复或视觉调整图标时，必须等待负责人提供或批准 SVG。
- 如果需求与原始 SVG 保护规则冲突，以保护规则为准，并向负责人说明阻塞原因。

## 完成标准

提交改动前至少确认：

- 未修改任何未经明确授权的 `packages/icons/raw/**/*.svg`。
- 未手工修改应由脚本生成的文件。
- TypeScript 类型检查通过。
- 图标包和文档网站可以正常构建。
- 公共 API 的变化已记录，并尽量保持向后兼容。
- 新增行为已通过必要的自动化或手工验证。
